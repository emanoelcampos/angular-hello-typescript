var minhaVar = 'minha variavel';

function minhaFuncao(x: number, y:number) {
    return x + y;
}

//ES 6 ou ES 2015
let num2 = 2;
const PI2 = 3.14;

var numeros = [1,2,3];
numeros.map(function(valor){
    return valor * 2;
});

numeros.map(valor => valor * 2)

class Matematica2 {
    soma(x:number, y:number) {
        return x + y;
    }
}

//explicação de tipagem de variáveis
var nome2 = 'emanoel';